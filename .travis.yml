language: generic
sudo: false
dist: trusty

notifications:
  email: false

env:
  matrix:
  - ARGS=""
  - ARGS="--resolver lts-2"
  - ARGS="--resolver lts-6"
  - ARGS="--resolver lts-9"
  - ARGS="--resolver lts-11"

addons:
  apt:
    packages:
      - libgmp-dev

before_install:
  - mkdir -p ~/.local/bin
  - export PATH=$HOME/.local/bin:$PATH
  - travis_retry curl -L https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'

install:
  - stack --no-terminal $ARGS build --test --only-dependencies

script:
  - stack --no-terminal $ARGS build --haddock --no-haddock-deps
  - stack --no-terminal $ARGS test

cache:
  directories:
    - $HOME/.stack
